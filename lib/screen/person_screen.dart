import 'package:flutter/material.dart';

class PersoneScreen extends StatelessWidget
{
  static String routName = 'persone_screen';
  const PersoneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
