import 'package:flutter/material.dart';

class TicketScreen extends StatelessWidget
{
  static String routName = 'ticket_screen';
  const TicketScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
